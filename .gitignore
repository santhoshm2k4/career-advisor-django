# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are generated by PyInstaller.
#  Most of BUNDLE section is Windows-specific.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
db.sqlite3       # Your SQLite database (contains user data, passwords - DO NOT UPLOAD)
db.sqlite3-journal # SQLite journal file
media/           # Usually user-uploaded files (if you had an ImageField for profile pic, etc.)
                 # Keep if your media files are generic and part of the app design.
                 # Exclude if they are user-specific or large.
staticfiles_collected/ # Files gathered by collectstatic, generated for deployment

# Environment variables
.env             # If you use python-dotenv for local environment variables
.env.*

# IDE / Editor specific files
.idea/           # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/         # Visual Studio Code (settings.json, etc.)
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# macOS
.DS_Store

# Optional: If your ml_models are very large and you plan to host them elsewhere (e.g., S3)
# ml_models/